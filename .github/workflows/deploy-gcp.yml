name: Deploy to GCP

on:
  push:
    branches: [main]

permissions:
  id-token: write
  contents: read

concurrency:
  group: deploy-gcp-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - id: auth
        uses: google-github-actions/auth@v2
        with:
          workload-identity-provider: projects/871583157548/locations/global/workloadIdentityPools/github-pool/providers/github-provider
          service-account: github-cicd@alphalead-ai.iam.gserviceaccount.com

      - uses: google-github-actions/setup-gcloud@v2

      - name: Submit Cloud Build
        run: |
          gcloud builds submit . \
            --config=cloudbuild.yaml \
            --project=alphalead-ai \
            --substitutions=_VERSION=${{ github.sha }}
